# =============================================================================
# fldcf — Docker 构建上下文忽略（仓库根为 context，backend / gp-front 共用）
# - Docker 不会读取 .gitignore，此处需单独维护。
# - 勿排除整个 backend/ 或 gp-front/，否则另一服务的 Dockerfile 中 COPY 会失败。
# - 大权重与 Git 一致：不进构建 tar，运行时由 compose 挂载 backend/fldcf_data。
# =============================================================================

# -----------------------------------------------------------------------------
# Git / 元数据（镜像不需要）
# -----------------------------------------------------------------------------
.git
**/.git
.gitattributes
.gitignore

# -----------------------------------------------------------------------------
# 密钥与环境文件（勿打进构建 tar）
# -----------------------------------------------------------------------------
**/.env
.env
.env.*
docker.env
!.env.example
!**/.env.example
!docker.env.example
.envrc
.env.local

# 日志（本地调试）
**/*.log

# -----------------------------------------------------------------------------
# Python：虚拟环境与缓存
# -----------------------------------------------------------------------------
**/.venv
**/venv
**/__pycache__
**/*.py[cod]
**/*.pyc
**/.pytest_cache
**/.mypy_cache
**/.ruff_cache
**/.tox
**/.nox
**/.hypothesis
**/.coverage
**/htmlcov
**/coverage.xml
**/*.egg-info

# -----------------------------------------------------------------------------
# Node：依赖与本地缓存（镜像内由 npm ci 重装）
# -----------------------------------------------------------------------------
**/node_modules
gp-front/dist
**/.vite
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# -----------------------------------------------------------------------------
# 文档与论文（两阶段镜像均不 COPY 这些路径）
# -----------------------------------------------------------------------------
docs/
*.md
!README.md

# -----------------------------------------------------------------------------
# FLDCF 权重（减小 context；运行时装载见 compose volumes）
# -----------------------------------------------------------------------------
backend/fldcf_data/**/*.pt
backend/fldcf_data/**/*.pth
backend/fldcf_data/**/*.ckpt
backend/fldcf_data/**/*.onnx
backend/fldcf_data/**/*.safetensors
backend/fldcf_data/experiment/

# -----------------------------------------------------------------------------
# IDE / 系统杂项
# -----------------------------------------------------------------------------
.idea
**/.idea
.vscode
**/.vscode
.DS_Store
Thumbs.db
Desktop.ini
